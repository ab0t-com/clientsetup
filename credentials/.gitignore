# Generated credentials — never commit these
*.json
*.json.bak.*

# But DO commit the example schema contracts
!*.json.example

# End-users OAuth client (RFC 7592 registration_access_token)
end-users-oauth-client*.json
